import { ref } from 'vue'

export const staffs = ref([])

export const pekerja = ref({})

export const promo = ref([])

export const mosi = ref({})

export const inven = ref([])

export const tori = ref({})